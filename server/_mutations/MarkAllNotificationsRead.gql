mutation MarkAllNotificationsRead($user_id: uuid) {
  update_notifications(where: { user_id: { _eq: $user_id }, read: { _eq: false } }, _set: { read: true }) {
    affected_rows
    returning {
      id
      read
    }
  }
}
