mutation GetMe($token: String!) {
  Me(input: { token: $token }) {
    token
    user {
      id
      email
      username
      FirstName
      LastName
      avatar_url
      created_at
      updated_at
    }
  }
}
