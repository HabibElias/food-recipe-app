mutation GetMe($token: String!) {
  Me(input: { token: $token }) {
    token
    user {
      id
      email
      username
      FirstName
      bio
      LastName
      avatar_url
      created_at
      updated_at
    }
  }
}
